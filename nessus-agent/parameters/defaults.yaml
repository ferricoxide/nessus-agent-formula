# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: nessus-agent
  rootgroup: root
  config: '/etc/nessus-agent'
  service:
    name: nessus-agent
  subcomponent:
    config: '/etc/nessus-agent-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
